# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# Secrets / credentials — never commit these
.env
*.pem
*.key

# ADK scratch / temp
_smoke_test.py
/tmp/

# Project scratch
.scratch/

# Playwright MCP extraction + verification artifacts (kept locally,
# regenerable from the live site or static mockup; ~4 MB binary blobs)
.playwright-mcp/

# Plan-mode scratch files (auto-generated by /plan)
witty-giggling-clover.md

# Submission deck (large binary, kept locally — not in git)
*.pptx
*.pptx.*

# OS / IDE
.DS_Store
.idea/
.vscode/
*.swp
